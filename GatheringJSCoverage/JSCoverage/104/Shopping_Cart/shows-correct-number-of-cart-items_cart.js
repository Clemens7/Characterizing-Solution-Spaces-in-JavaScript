class Product {
    /**
     * Create JSON Object <code>Product</code>
     * 
     * @param objectID id of an art-item
     * @param printSize size can be 'S' or 'M' or 'L'
     * @param frameStyle style can be 'classic' or 'natural' or 'shabby' or 'elegant'
     * @param frameWidth in mm from 20 to 50
     * @param matColor in RGB '#XXXXXX'
     * @param matWidth in mm from 0 to 100
     */
    
}

/**
 * loads an array of Products into a given storage
 * 
 * @param store store function of a target storage.
 */
export 

/**
 * loads an empty cart into a given storage.
 * 
 * @param store store function of a target storage.
 */
export 