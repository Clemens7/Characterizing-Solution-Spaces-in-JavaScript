/**
 * JQuery-esque style querySelector (just for laziness' sakes). Wrapper for document.querySelector
 * @param selector is the querySelector to determine the element
 * @returns the element that matches the selector
 */
export function $(selector) {
    return document.querySelector(selector);
}

/**
 * JQuery-esque style querySelector (just for laziness' sakes). Wrapper for document.querySelectorAll
 * @param selector is the querySelector to determine the elements
 * @returns the elements that matches the selector
 */
export 
